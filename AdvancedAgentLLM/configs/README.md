# Experiment configurations

The files here reserve separate configuration boundaries for base-model
pretraining, supervised fine-tuning, and the eventual agent runtime. They stay
as placeholders until each lesson reaches that stage.

