# Entry points

- `train_tokenizer.py` trains the byte-level BPE tokenizer.
- `pretrain.py` launches base-model pretraining and supports checkpoint resume.
- `sft.py` remains the future supervised-fine-tuning entry point.
- `run_agent.py` remains the future model/tool runtime entry point.
