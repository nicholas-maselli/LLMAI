# Experiment configurations

- `model/small.yaml` will describe the development-scale architecture.
- `model/base.yaml` will describe a larger architecture.
- `model/large.yaml` will describe the largest planned architecture.
- `pretrain.yaml` will contain data, optimization, logging, and checkpoint settings.

These are placeholders until we discuss how configuration composition should work.
